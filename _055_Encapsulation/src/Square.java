public class Square {
    private int length;

    public int area(int length){
        return length * length;
    }
    // this is a encapsulation too.Because we are bundling data and methods in one.

}
